# Dependencies
node_modules/
.pnpm-store/

# Build output (public/ is the Signal K webapp build dir, shipped to npm via files, not committed)
public/
dist/
.vite/
*.tsbuildinfo

# npm pack output
*.tgz

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment and local-only files
.env
.env.*
!.env.example
*.local

# Test and coverage artifacts
coverage/
playwright-report/
playwright-results/
test-results/
.playwright-mcp/
.vitest-attachments/

# Tooling caches
.cache/
.tmp/
.svelte-check/

# Editors and IDEs
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Scratch directory for screenshots, throwaway debug scripts, and captured logs.
/tmp/

# Local debug screenshots dropped at the repo root during live verification. Real app and store
# screenshot sources live under static/screenshots/ and are copied into the generated public/ output.
/*.png

# Failure screenshots a red Vitest browser-mode run writes beside the test file inside src/.
__screenshots__/

# Agent and session scratch (local only, not for contributors)
.claude/
.superpowers/
.remember/
CLAUDE.local.md

# Design specs and build plans (local working notes, not committed)
docs/superpowers/

# Agent-session infrastructure (in-repo worktrees from the cave plugin)
.cave/
